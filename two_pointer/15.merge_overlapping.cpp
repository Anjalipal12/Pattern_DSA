#include<bits/stdc++.h>
using namespace std;

vector<vector<int>>intervals(vector<vector<int>> &intervals){
    vector<int>ans;
    int n = intervals.size();
    sort(intervals.begin(),intervals.end());
    current intervals 
    if(ans.empty() == 0 || interv)

}
int main(){
    vector<vector<int>> intervals = {{1,3},{2,6},{8,9},{9,11},{8,10},{2,4},{15,18},{16,17}};
    vector<vector<int>>merge_overlapping;

}